import 'package:flutter/material.dart';

class UserAppoint extends StatelessWidget {
  const UserAppoint({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}